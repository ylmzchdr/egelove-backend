"use client";

import React, { useEffect, useState } from "react";
import Link from "next/link";
import {
  Camera,
  Heart,
  MapPin,
  MessageCircle,
  Search,
  Sparkles,
  UserRound,
  ChevronRight,
  Users,
  CheckCircle2,
} from "lucide-react";

import Sidebar from "@/components/dashboard/Sidebar";
import Topbar from "@/components/dashboard/Topbar";

type DashboardUser = {
  name?: string;
  username?: string;
  city?: string;
  profilePhoto?: string | null;
  isPremium?: boolean;
};

const onlineUsers = [
  {
    name: "Sabrina",
    city: "Muğla",
    initials: "SA",
    ring: "from-pink-500 to-purple-600",
  },
  {
    name: "Can",
    city: "Muğla",
    initials: "CA",
    ring: "from-blue-500 to-cyan-500",
  },
  {
    name: "Merve",
    city: "Aydın",
    initials: "ME",
    ring: "from-purple-500 to-pink-500",
  },
  {
    name: "Deniz",
    city: "Antalya",
    initials: "DE",
    ring: "from-emerald-500 to-cyan-500",
  },
  {
    name: "Elif",
    city: "İstanbul",
    initials: "EL",
    ring: "from-orange-500 to-amber-500",
  },
  {
    name: "Burak",
    city: "Ankara",
    initials: "BU",
    ring: "from-indigo-500 to-purple-500",
  },
  {
    name: "Zeynep",
    city: "Diyarbakır",
    initials: "ZE",
    ring: "from-rose-500 to-pink-500",
  },
  {
    name: "Hakan",
    city: "Trabzon",
    initials: "HA",
    ring: "from-cyan-500 to-blue-500",
  },
];

export default function DashboardPage() {
  const [user, setUser] = useState<DashboardUser>({});
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);

  useEffect(() => {
    try {
      const rawUser = localStorage.getItem("user");

      if (rawUser) {
        const parsed = JSON.parse(rawUser);

        setUser({
          name:
            parsed?.name ||
            parsed?.firstName ||
            parsed?.username ||
            "Üye",
          username: parsed?.username,
          city:
            parsed?.city?.name ||
            parsed?.city ||
            "Türkiye",
          profilePhoto:
            parsed?.profilePhoto ||
            parsed?.avatar ||
            parsed?.photoUrl ||
            null,
          isPremium: Boolean(parsed?.isPremium),
        });
      }
    } catch {
      setUser({});
    }
  }, []);

  return (
    <div className="min-h-screen bg-[#080b14] text-white">
      <div className="flex min-h-screen">
        {/* DESKTOP SIDEBAR */}
        <aside className="hidden lg:block lg:w-[286px] lg:shrink-0">
          <div className="sticky top-0 h-screen">
            <Sidebar />
          </div>
        </aside>

        {/* MOBILE MENU */}
        {mobileMenuOpen && (
          <div className="fixed inset-0 z-[100] lg:hidden">
            <button
              type="button"
              aria-label="Menüyü kapat"
              onClick={() => setMobileMenuOpen(false)}
              className="absolute inset-0 bg-black/70 backdrop-blur-sm"
            />

            <div className="relative z-10 h-full w-[286px]">
              <Sidebar />
            </div>
          </div>
        )}

        {/* ANA ALAN */}
        <main className="min-w-0 flex-1">
          <div className="mx-auto w-full max-w-[1500px] px-4 py-4 sm:px-6 lg:px-8 lg:py-6">

            {/* ÜST BAR */}
            <Topbar
              userName={user.name || "Üye"}
              userCity={user.city || "Türkiye"}
              profilePhoto={user.profilePhoto || null}
              isPremium={user.isPremium || false}
              unreadMessages={0}
              unreadNotifications={0}
              onOpenMobileMenu={() => setMobileMenuOpen(true)}
            />

            {/* BAŞLIK */}
            <section className="mb-6">
              <div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between">
                <div>
                  <h1 className="text-3xl font-black tracking-tight text-white sm:text-4xl">
                    EGE <span className="text-purple-400">LOVE</span>
                  </h1>

                  <p className="mt-2 text-sm text-slate-400 sm:text-base">
                    Türkiye'nin dört bir yanından yeni insanlarla tanış.
                  </p>
                </div>

                <div className="flex items-center gap-2 text-xs font-semibold text-emerald-400">
                  <span className="h-2 w-2 animate-pulse rounded-full bg-emerald-400" />
                  PLATFORM AKTİF
                </div>
              </div>
            </section>

            {/* HIZLI AKSİYONLAR */}
            <section className="mb-7 grid grid-cols-1 gap-4 md:grid-cols-2">

              <Link
                href="/profile/edit"
                className="group rounded-3xl border border-purple-500/30 bg-gradient-to-r from-purple-600/20 to-pink-500/10 p-5 transition-all duration-300 hover:border-purple-400/60 hover:from-purple-600/30 hover:to-pink-500/15"
              >
                <div className="flex items-center justify-between">
                  <div className="flex items-center gap-4">
                    <div className="flex h-12 w-12 items-center justify-center rounded-2xl border border-purple-400/30 bg-purple-500/20">
                      <UserRound className="h-6 w-6 text-purple-300" />
                    </div>

                    <div>
                      <h2 className="text-base font-bold text-white">
                        Profilini Doldur
                      </h2>

                      <p className="mt-1 text-sm text-slate-400">
                        Profilini tamamla, seni daha kolay keşfetsinler.
                      </p>
                    </div>
                  </div>

                  <ChevronRight className="h-5 w-5 text-purple-400 transition-transform group-hover:translate-x-1" />
                </div>
              </Link>

              <Link
                href="/search"
                className="group rounded-3xl border border-white/10 bg-[#0d1220]/80 p-5 transition-all duration-300 hover:border-purple-500/40 hover:bg-purple-500/10"
              >
                <div className="flex items-center justify-between">
                  <div className="flex items-center gap-4">
                    <div className="flex h-12 w-12 items-center justify-center rounded-2xl border border-purple-400/20 bg-purple-500/10">
                      <Search className="h-6 w-6 text-purple-300" />
                    </div>

                    <div>
                      <h2 className="text-base font-bold text-white">
                        Aradığın Kişiyi Bul
                      </h2>

                      <p className="mt-1 text-sm text-slate-400">
                        Şehir, yaş ve diğer filtrelerle ara.
                      </p>
                    </div>
                  </div>

                  <ChevronRight className="h-5 w-5 text-purple-400 transition-transform group-hover:translate-x-1" />
                </div>
              </Link>

            </section>

            {/* ÇEVRİMİÇİ ÜYELER */}
            <section className="mb-7 rounded-3xl border border-white/10 bg-[#0d1220]/80 p-5 shadow-2xl sm:p-6">
              <div className="mb-5 flex items-center justify-between">
                <div className="flex items-center gap-2">
                  <span className="h-2.5 w-2.5 animate-pulse rounded-full bg-emerald-400" />

                  <h2 className="text-sm font-bold uppercase tracking-widest text-emerald-400">
                    81 İLDEN CANLI ÇEVRİMİÇİ ÜYELER
                  </h2>
                </div>

                <Link
                  href="/search"
                  className="text-xs font-semibold text-slate-500 transition-colors hover:text-purple-400"
                >
                  Tümünü Gör →
                </Link>
              </div>

              <div className="grid grid-cols-4 gap-y-6 sm:grid-cols-6 lg:grid-cols-8">
                {onlineUsers.map((member) => (
                  <Link
                    key={member.name}
                    href="/search"
                    className="group flex flex-col items-center"
                  >
                    <div
                      className={`relative h-14 w-14 rounded-full bg-gradient-to-tr ${member.ring} p-[2px] transition-transform duration-300 group-hover:scale-105`}
                    >
                      <div className="flex h-full w-full items-center justify-center rounded-full bg-[#0b0f1b]">
                        <span className="text-xs font-bold text-slate-400">
                          {member.initials}
                        </span>
                      </div>

                      <span className="absolute bottom-0 right-0 h-3.5 w-3.5 rounded-full border-2 border-[#0b0f1b] bg-emerald-400" />
                    </div>

                    <span className="mt-2 text-xs font-bold text-slate-200">
                      {member.name}
                    </span>

                    <span className="mt-0.5 text-[10px] text-slate-500">
                      {member.city}
                    </span>
                  </Link>
                ))}
              </div>
            </section>

            {/* ANA KARTLAR */}
            <section className="mb-7 grid grid-cols-1 gap-5 md:grid-cols-2">

              <Link
                href="/messages"
                className="group relative overflow-hidden rounded-3xl border border-purple-500/30 bg-gradient-to-br from-purple-900/40 to-[#0d1220] p-6 transition-all duration-300 hover:border-purple-400/50"
              >
                <div className="absolute -right-12 -top-12 h-36 w-36 rounded-full bg-purple-500/10 blur-3xl transition-all group-hover:bg-purple-500/20" />

                <div className="relative">
                  <div className="mb-4 flex items-center gap-4">
                    <div className="flex h-12 w-12 items-center justify-center rounded-2xl border border-purple-400/30 bg-purple-500/20">
                      <Camera className="h-6 w-6 text-purple-300" />
                    </div>

                    <div>
                      <h3 className="text-base font-bold text-white">
                        Sesli & Görüntülü Canlı Sohbet
                      </h3>

                      <span className="mt-1 inline-flex items-center gap-2 rounded-full border border-emerald-400/30 bg-emerald-400/10 px-3 py-1 text-[10px] font-bold tracking-wider text-emerald-400">
                        <span className="h-1.5 w-1.5 rounded-full bg-emerald-400" />
                        AKTİF & CANLI SİSTEM
                      </span>
                    </div>
                  </div>

                  <p className="text-sm leading-7 text-slate-300">
                    Türkiye'nin 81 ilinden yeni insanlarla sesli ve görüntülü
                    sohbet et. Anlık ve kesintisiz canlı sohbet deneyimi burada.
                  </p>
                </div>
              </Link>

              <div className="relative overflow-hidden rounded-3xl border border-white/10 bg-[#0d1220]/80 p-6">
                <div className="mb-4 flex items-center gap-4">
                  <div className="flex h-12 w-12 items-center justify-center rounded-2xl border border-blue-400/20 bg-blue-500/10">
                    <Sparkles className="h-6 w-6 text-blue-300" />
                  </div>

                  <div>
                    <h3 className="text-base font-bold text-white">
                      Google Play & App Store
                    </h3>

                    <span className="mt-1 inline-block rounded-md border border-blue-400/20 bg-blue-500/10 px-2 py-1 text-[10px] font-bold tracking-wider text-blue-400">
                      GELİŞTİRİLİYOR
                    </span>
                  </div>
                </div>

                <p className="text-sm leading-7 text-slate-400">
                  Egelove mobil uygulamaları için çalışmalar devam ediyor.
                  Yakında mağazalarda.
                </p>
              </div>

            </section>

            {/* ALT BİLGİ KARTLARI */}
            <section className="grid grid-cols-2 gap-4 lg:grid-cols-4">

              <Link
                href="/likes"
                className="rounded-2xl border border-white/10 bg-[#0d1220]/80 p-5 transition-all hover:border-pink-500/30"
              >
                <Heart className="mb-8 h-5 w-5 text-pink-400" />

                <span className="block text-sm font-bold text-white">
                  Yeni Beğeni
                </span>

                <span className="mt-1 flex items-center gap-1 text-[11px] font-semibold text-emerald-400">
                  <span>•</span> Aktif
                </span>
              </Link>

              <Link
                href="/messages"
                className="rounded-2xl border border-white/10 bg-[#0d1220]/80 p-5 transition-all hover:border-blue-500/30"
              >
                <MessageCircle className="mb-8 h-5 w-5 text-blue-400" />

                <span className="block text-sm font-bold text-white">
                  Okunmamış Mesaj
                </span>

                <span className="mt-1 block text-[11px] text-slate-500">
                  MESAJLAR
                </span>
              </Link>

              <Link
                href="/search"
                className="rounded-2xl border border-white/10 bg-[#0d1220]/80 p-5 transition-all hover:border-purple-500/30"
              >
                <Users className="mb-6 h-5 w-5 text-purple-400" />

                <span className="block text-3xl font-black text-white">
                  81
                </span>

                <span className="mt-1 block text-[11px] text-slate-500">
                  İlde Üye Ara
                </span>
              </Link>

              <Link
                href="/search"
                className="rounded-2xl border border-white/10 bg-[#0d1220]/80 p-5 transition-all hover:border-amber-500/30"
              >
                <MapPin className="mb-6 h-5 w-5 text-amber-400" />

                <span className="block text-sm font-bold text-white">
                  Türkiye
                </span>

                <span className="mt-1 block text-[11px] text-slate-500">
                  Konumuna Göre Ara
                </span>
              </Link>

            </section>

            {/* GÜVEN MESAJI */}
            <div className="mt-6 flex items-center justify-center gap-2 pb-4 text-center text-[11px] text-slate-500">
              <CheckCircle2 className="h-4 w-4 text-emerald-400" />
              Güvenli bağlantı • Egelove
            </div>

          </div>
        </main>
      </div>
    </div>
  );
}